// Core module: Displays index

define(['jquery'], function() {
    function makeIndex(modules) {
        var $index = $('<div></div>');
        $.each(modules, function(key, value) {
        
        })
    }

    function init(modules) {
        // look for index placeholder
    }
    return { init: init };
});